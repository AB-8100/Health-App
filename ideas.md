# Raw feature ideas — drop bullets here, any format, the planning agent sorts them

- Cycle-phase-aware training load adjustment (ease intensity suggestions in luteal phase)
- Monthly AI nudge — pattern-based tip from a month of logged data
- Manual quick-entry logging for runs/rides (distance, duration, pace, RPE — no GPS)
- Rolling weekly "relative effort" load score, replacing placeholder overtrain checker
- Live map + route tracking for runs
- Streaks and badges for consecutive logged gym sessions
- Wire HomeScreen's activity rings to real logged data instead of demo data
- Garmin Connect API prototype — pull own activity + Women's Health cycle data
- In-app coach chat that nudges users to log workouts they've skipped
- Strip triathlon-specific onboarding assumptions into configurable defaults
